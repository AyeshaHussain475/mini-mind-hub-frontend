import { useState } from "react";

const useAuth = async () => {
  const [isLoading, setIsLoading] = useState(true);
  const [auth, setAuthenticate] = useState("");
  const [error, setError] = useState("");
};
